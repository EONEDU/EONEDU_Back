spring:
  config:
    import: optional:file:.env[.properties]
  jpa:
    properties:
      hibernate:
        show_sql: ${TEST}
        format_sql: true
        default_batch_fetch_size: 100
    open-in-view: false