spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:localDB;MODE=MYSQL
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create  # local 환경에서만 create 사용하기 !!!